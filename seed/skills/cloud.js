// seed/skills/cloud.js — Vercel, Firebase Hosting
import { mk } from '../helpers.js';

export default function buildCloudSkills() {
  return [
    mk('Vercel', 'cloud'),
    mk('Firebase Hosting', 'cloud'),
  ];
}
