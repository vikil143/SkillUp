// seed/skills/styling.js — Tailwind, MUI, Styled Components, Bootstrap, CSS Modules
import { mk } from '../helpers.js';

export default function buildStylingSkills() {
  return [
    mk('Tailwind CSS', 'styling'),
    mk('Material UI', 'styling'),
    mk('Styled Components', 'styling'),
    mk('Bootstrap', 'styling'),
    mk('CSS Modules', 'styling'),
  ];
}
